import * as React from 'react';
import styled from 'styled-components';

//Styles:
const MainContainer = styled.div`
    border: 1px solid black;
    padding: 2em 1em;
`;

const GlobalSearchSectionFour = () => {
    return (
        <MainContainer>
            This section will probably be for finding categories of instant
            ramen by different countries.
        </MainContainer>
    );
};

export default GlobalSearchSectionFour;
