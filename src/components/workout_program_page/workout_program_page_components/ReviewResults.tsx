import * as React from 'react';

//Components:

//Styles:
import styled from 'styled-components';

const MainContainer = styled.div`
    padding: 1rem 1rem;
`;

//Interfaces:

const ReviewResults = () => {
    return <MainContainer>This should be review results</MainContainer>;
};

export default ReviewResults;
