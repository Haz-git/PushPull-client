import * as React from 'react';
import styled from 'styled-components';

//Styles:
const MainContainer = styled.div`
    border: 1px solid black;
    padding: 1rem 1rem;
`;

const index = () => {
    return <MainContainer>RamenRankings (Logo)</MainContainer>;
};

export default index;
