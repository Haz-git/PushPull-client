import * as React from 'react';

//Components:

//Styles:

//Interfaces:

const MainBuildTemplateView = () => {
    return (
        <div>
            This should be the page that allows for users to build and create
            own workout templates.
        </div>
    );
};

export default MainBuildTemplateView;
