version: '3.8'
services:
    frontend:
        build: ./
        container_name: pushpull_client
        ports:
            - '3000:3000'
        volumes:
            - './:/app'
        environment:
            - NODE_ENV=development
        stdin_open: true
        tty: true
